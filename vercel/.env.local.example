# Copy the variables you need into a root-level `.env.local` when running `vercel dev`
# (Vercel CLI reads env from the project root, not this subfolder).

# Core app data sources
SOLANA_RPC_URL=https://your-rpc-url
JUPITER_API_KEY=your_jupiter_api_key

# Optional overrides used by your existing codebase (keep defaults unless needed)
# KAMINO_BASE_URL=https://api.kamino.finance
# KAMINO_ENV=mainnet-beta
# ONRE_LIVE_APY_URL=https://core.api.onre.finance/data/live-apy
# POSITIONS_CACHE_TTL_MS=15000

# Notify route (Telegram)
WALLET_ADDRESS=4ogWhtiSEAaXZCDD9BPAnRa2DY18pxvF9RbiUUdRJSvr
TELEGRAM_BOT_TOKEN=123456789:replace_me
TELEGRAM_CHAT_ID=123456789
NOTIFY_TOKEN=replace_with_random_secret
